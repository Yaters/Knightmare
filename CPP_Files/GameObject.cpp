#include "GameObject.h"

GameObject::GameObject(glm::vec2 Pos, glm::vec2 Size, glm::vec2 Dir):
	pos(Pos), size(Size), dir(Dir) {
	//lol also I did the colon thing because idk any way to format it well, so plooga plooga your way outta here
}
GameObject::GameObject() {

}